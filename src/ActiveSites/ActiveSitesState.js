"use strict";

var ActiveSitesState;

ActiveSitesState = function(state) {
    state = state || {};
    this.visible = state.visible || false ;
    this.visible = state.visible || false ;
    this.visible = state.visible || false ;
    this.visible = state.visible || false ;
    this.visible = state.visible || false ;
};

if (typeof module != "undefined")
    module.exports = ActiveSitesState;